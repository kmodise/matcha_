<!DOCTYPE html>
<html lang="en">

<%
var i = 0; while (users[i]) {

i++;

}

%>

<body>
    <% include ../extras/navbar.ejs %>


  <div>

    <div>
      <div>
        <div>
          <% var i = 0; while (users[i]) { %>
          <div>
            <div onclick="location.href='<%='/user_profile/' + users[i].id%>'">
              <img src="<%= users[i].profileImg %>">
              <div>
                <h5><%=users[i].firstname%> <%= users[i].lastname%><%=users[i].age%> years old</h5>
                <p><%= users[i].bio%></p>
              </div>
            </div>
          </div>
        <% i++; } %>
      </div>
    </div>
  </div>

  <% include ../extras/footer.ejs %>
</body>

</html>